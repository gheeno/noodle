# NOOD_0216 — the api wok's engine: stdlib REST transport (rest_client) and
# the protocol-level actions the runner's _execute_rest dispatches into
# (actions). Browserless by construction — nothing here may import Playwright.
