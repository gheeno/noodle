# Copy to practicetestautomation_secrets.env (gitignored — never commit it) and fill in.
# Referenced as [KEY] placeholders in this app's features/*.feature files.
# Package-scoped — see docs/feature-packages.md for the resolution order.

PTA_USERNAME=student
PTA_PASSWORD=Password123
