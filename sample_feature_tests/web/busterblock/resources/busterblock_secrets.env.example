# Copy to busterblock_secrets.env (gitignored — never commit it) and fill in.
# Referenced as [BB_USER] etc. in this app's features/*.feature files.
# Package-scoped — see ../../../../docs/feature-packages.md for the resolution order.

BB_USER=reel_ryan
BB_PASS=Popcorn1!
